/*You can new package in the file and folder and then make them classes to be  transferred in the package which we created as a whole new in the program
 Advantage of having a package, we can provide a whole new class name to the package so it will be easy to extract the packages
 */

pacakage packexample;
import packexample.dao;
import
public class PackExample
{

	public static void main(String args[])
	{
		AccDAO obj=new AccDAO();
	
	}
}
